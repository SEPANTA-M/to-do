# NEXUS environment
# Copy to .env.local for local development. Never commit secrets.

# Optional PostgreSQL connection for a future sync worker.
# The app is fully usable without this — persistence is IndexedDB.
# DATABASE_URL=postgresql://user:password@localhost:5432/nexus

# NODE_ENV is set by Next.js. Do not put API keys in client code.
# NODE_ENV=development
